# Generate a map

To generate a tidy html / graphical representation of the map, use
the following:

`uvx --from keymap-drawer keymap draw lily58.yaml`
`uvx --from keymap-drawer keymap draw lily58.yaml -o out.html`
